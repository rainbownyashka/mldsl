# `if_value.peremennaya_suschestvuet(var: VARIABLE (slot 13), var2: VARIABLE (slot 31))`

- `aliases`: `peremennaya_suschestvuet`, `Переменная существует`, `переменная_существует`
- `sign1`: `Если переменная`
- `sign2`: `Переменная существует`
- `gui`: `Переменная существует`
- `id`: `||[minecraft:slime_ball meta=0] переменная существует | проверяет, есть ли в игре данная переменная. \n \n в сундуке: \n динамическая переменная - \n переменная для проверки. \n \n данное действие тебе доступно!|переменная существует|если значение|переменная существует||`

## Description
```
Проверяет, есть ли в игре данная переменная.  
  
 В сундуке:  
 Динамическая переменная -  
 переменная для проверки.  
  
 Данное действие тебе доступно!
```

## DescriptionRaw
```
§7Проверяет, есть ли в игре данная переменная. §r 
  
 §fВ сундуке: §r 
 §cДинамическая переменная §8-  
 §7переменная для проверки. §r 
  
 §aДанное действие тебе доступно!
```

## Params
- `var`: `VARIABLE` slot `13`
- `var2`: `VARIABLE` slot `31`

