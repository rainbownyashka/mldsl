Задача: сделай файл `main.mldsl` в папке сессии.
Сначала проверь API через инструменты, и только потом пиши код (не пиши MLDSL до проверок).

НЕЛЬЗЯ:
- придумывать модули/функции/аргументы/события
- писать `&&`, `event(join)`, `if (...)` как в других ЯП

НАДО:
- Перед тем как писать код, проверь, что нужные вещи существуют:
  - list_modules
  - list_funcs(module, preview_args=2, limit=25) (не запрашивай полный список без limit)
  - resolve_target/get_sig для каждого используемого действия
- Пиши MLDSL только в синтаксисе: `event(\"...\") { ... }` и `module.func(...) { ... }`
- После записи файла обязательно: check_compilation(path)

Требование GM-палки:
- event(\"Вход игрока\"): if_player.* (право=\"Белый список\") → выдать палку (minecraft:stick) с именем (например §6Палка)
- event(\"Игрок кликает правой кнопкой\"): if_player.держит(палку) → установить режим игры \"Креатив\"
- event(\"Игрок кликает левой кнопкой\"): if_player.держит(палку) → установить режим игры \"Выживание\"
