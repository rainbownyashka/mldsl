Сделай файл `main.mldsl`.

Задача: “GM палка” (Magic stick).

Требования:
- При входе игрока: если у игрока есть право `"Белый список"` (if_player.имеет_право), выдать предмет `minecraft:stick` с количеством 1 и именем `§6GM Stick`.
- При событии `"Игрок кликает правой кнопкой"`: если игрок держит эту палку (if_player.держит), то установить режим игры `"Креатив"`.
- При событии `"Игрок кликает левой кнопкой"`: если игрок держит эту палку (if_player.держит), то установить режим игры `"Выживание"`.

Ограничения (строго):
1) Пиши только tool calls (builder-mode). Никакого “обычного” текста.
2) Перед генерацией кода узнай реальные имена функций через tools:
   `list_modules` -> `list_funcs(module)` -> `get_sig` для нужных функций/условий.
3) Не выдумывай функций и аргументов. Используй только то, что показывает `get_sig`.
4) После генерации проверь `validate_mldsl`, затем `finalize_program` (или `save_program`).

Подсказка по инструментам:
- `new_program(path="...")` / `create_file(path="...", language="mldsl")`
- `begin_event(name="...") ... end_event()`
- `begin_if(module="if_player", func="...", kwargs={...}) ... end_if()`
- `add_action(module="player", func="...", kwargs={...})`
- `finalize_program(file_id="...")`

