Задача: сгенерируй файл `main.mldsl` (пишем на MLDSL) для “GM палки”.

Логика:
- `event("Вход игрока")`:
  - если игрок имеет право `"Белый список"`, выдать предмет: палка `minecraft:stick` с именем `§6GM Stick` (count=1)
- `event("Игрок кликает правой кнопкой")`:
  - если игрок держит эту палку (minecraft:stick + name=§6GM Stick), установить режим игры `"Креатив"`
- `event("Игрок кликает левой кнопкой")`:
  - если игрок держит эту палку (minecraft:stick + name=§6GM Stick), установить режим игры `"Выживание"`

ВАЖНО: отвечай ТОЛЬКО tool-calls (никакого текста/объяснений).

Формат tool-call (строго):
<tool_call>{"name":"tool_name","arguments":{...}}</tool_call>

Порядок работы:
1) Сначала вызови `session_info`
2) Затем `get_sig` для нужных блоков (минимум эти):
   - if_player.имеет_право
   - if_player.держит
   - player.выдать_предметы
   - player.установить_режим_игры
3) Потом `write_file`:
   - `path` = sessionRel + "main.mldsl"
   - `content` = готовый MLDSL код (без импорта, без лишнего)
4) Затем `check_compilation` на этот файл

Ограничения:
- Не выдумывай новые ключевые слова/синтаксис. Используй блоки как `event(...) { ... }`, `if_player.xxx(...) { ... }`, `player.xxx(...)`.
- Не используй `иначе`.
- Не делай “модульных” вызовов типа `game.xxx`, если их не просили.
