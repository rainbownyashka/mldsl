Сделай файл `main.mldsl`.

Требование: используй ТОЛЬКО tool calls (builder mode). Не пиши сырой код напрямую.

Задача (GM контроль без упоминания палки):
1) Событие: "Вход игрока"
2) Если игрок имеет право "Белый список":
   - Отправить сообщение: "GM доступен."
   - Установить режим игры: "Креатив"
3) Иначе:
   - Отправить сообщение: "Нет прав."

Обязательные требования:
- Не выдумывай функции/модули. Всегда проверяй через `list_modules` -> `list_funcs` -> `get_sig`.
- Enum значения бери только из `get_sig` (если не совпадает — исправь).
- После сборки вызови `finalize_program(file_id)` и добейся, чтобы компиляция была ok=true и emptyPlan=false.
